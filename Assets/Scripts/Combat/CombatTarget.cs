using UnityEngine;
using RPG.Core;
using RPG.Attribiutes;

namespace RPG.Combat
{
    // Protect from removing component
    [RequireComponent(typeof(Health))]
    public class CombatTarget : MonoBehaviour
    {
        
    }
}